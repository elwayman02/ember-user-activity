export declare class CompiledBlock {
    start: number;
    end: number;
    constructor(start: number, end: number);
}
export declare class CompiledProgram extends CompiledBlock {
    symbols: number;
    constructor(start: number, end: number, symbols: number);
}
