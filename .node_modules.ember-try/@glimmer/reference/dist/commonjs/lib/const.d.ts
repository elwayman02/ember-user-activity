import { VersionedReference, RevisionTag } from './validators';
import { Opaque } from '@glimmer/util';
export declare class ConstReference<T> implements VersionedReference<T> {
    protected inner: T;
    tag: RevisionTag;
    constructor(inner: T);
    value(): T;
}
export declare function isConst(reference: VersionedReference<Opaque>): boolean;
